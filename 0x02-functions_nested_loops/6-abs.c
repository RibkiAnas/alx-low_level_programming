#include "main.h"

/**
 * _abs - computes the absolute value of an integer n.
 * @i: The number to computes
 *
 * Return: Absolute value.
*/
int _abs(int i)
{
	if (i >= 0)
		return (i);
	return (-i);
}
